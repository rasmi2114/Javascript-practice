// Math.round()  Round to the nearest interer
    // console.log(Math.round(4.1)); 
    // console.log(Math.round(4.5));  

// Math.floor()  Always rounds to the nearest integer.
    // console.log(Math.floor(4.1));
    // console.log(Math.floor(4.9));  

// Math.ceil()  always rounds up to the nearest integer.
    // console.log(Math.ceil(4.2));
    // console.log(Math.ceil(4.7));

// math.abs()  return the absolute value of the number (how far the no. is from 0)
   // console.log(Math.abs(5));
    //console.log(Math.abs(-9));

// Math.flooe(x) return the value of x rounded down to its nearest integer.
    // const floorvale = Math.floor(3.7);
    // console.log(floorvale);

 // Math.trunc(x) return the integer part  of x.
    // const truncvale = Math.trunc(3.7);
    // console.log(truncvale);
   
//Math.random() generate the random no between 0 and 1
    //console.log(Math.random());
    //console.log(Math.round(Math.random() * 100));  // to avoid the decimal no.
   // console.log((Math.random() * 100).toFixed(3));  // need 3 decimal point data.
